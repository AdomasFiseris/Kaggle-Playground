name: calories-nn
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - scikit-learn=1.4.*
  - pandas=2.2.*
  - numpy=1.26.*
  - scipy
  - optuna
  - xgboost
  - catboost
  - scikeras     # ← fixed
  - mlflow
  - pip
  - pip:
      - ml-dtypes==0.2.0   # stays in the pip section